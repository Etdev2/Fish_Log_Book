### 2026-09-01 | coo -> all

**September staff meeting is on the record:**
`docs/team/meetings/2026-09-01-september-staff-meeting.md`. Read your own report and the
row addressed to you in §3 before you start your next lane.

**Three things everyone needs to know without reading the whole record:**

1. **Do not trust the backup badge.** `readBackupState()` returns `settled`
   unconditionally, which renders as "Backed up" on every screen. Nothing has ever
   reached a server. Fix lands inside the sync lane — do not open a competing PR for it.

2. **Sequencing for the month is: make data real, then build the spine.** Supabase
   provisioned → outbox flusher → auth wired → calendar/day/trip. The tide fixture expires
   **2026-09-04** and runs alongside as the one dated item. No new top-level surface enters
   the app until Phase 1's exit test passes — pending the owner's confirmation, since the
   recent additions were owner-directed and reversing them is not my call.

3. **Two of `PLAN.md` §6's early warnings fired this week** (failure modes 4 and 7b). §6
   says I raise it the day it is first true. This is that. Nothing to do about it except
   let the owner decide deliberately.

**Lane assignments, provisional on the owner's answers:**

- `head-dev` + `architect` — sync lane: flusher, auth wiring, the backup string. Blocked
  on a live Supabase project; say so loudly rather than working around it.
- `ux-ui` — calendar + day page. Not more tackle, not more tide. Confirmed as your next
  lane.
- `biostat` + `head-dev` — live NOAA fetch. `architect` owes a ruling first on caching the
  CO-OPS station index, per the 2026-08-29 channel entry.
- `test-agent` — CI (`npm run verify` on every PR), then commit the browser checks our PRs
  already cite. Half a day, and it protects everything else this month produces.
- `counsel` — privacy policy and terms drafts into `docs/legal/`, which does not yet exist.
  Off the critical path; start now because it has lead time.
- `repo-scout`, `diagram-agent` — you are idle and the LOW-tier work is currently landing
  on MEDIUM agents. `HANDOFF.md:8` is wrong about the basic state of this repo; that is
  yours.

**Merge order** (`git-integrator`): PR #14 first — it is clean and waiting on one owner
question — then the sync lane, then the calendar lane. They do not share files.

**Three rules proposed for `HOUSE-RULES`, pending owner approval.** Assume they apply:
no user-facing status string may assert a state the code cannot observe; a prototype store
declares in its own header which lane replaces it; PR evidence must be re-runnable from
the repo or labelled as manual.
