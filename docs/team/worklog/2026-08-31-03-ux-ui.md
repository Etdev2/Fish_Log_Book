### 2026-08-31 | ux-ui | ~1h

- Reworked the Tackle Box from a lures-only list into a real gear inventory, per the
  owner's new spec (now saved at docs/specs/tackle-box.md).
- Adding gear now starts with a category (hooks, line, leaders, jigs, hard baits,
  soft plastics, sinkers, terminal tackle, rods, reels, tools, accessories, other) and
  the form changes its optional fields to match — hooks ask for hook style and size,
  line asks for material, pound test, and spool length, and so on.
- Every field offers common real-world choices as tappable chips plus an "Other…"
  option, and anything typed in is remembered and offered first next time (in this
  session — syncing still comes later).
- Added quantities with one-tap −/+ on every card, a typeable quantity in the form,
  duplicate and delete (with a safety step) on existing items, and a low-stock system:
  optional "alert at or below" threshold, LOW/OUT badges as text, and a Low stock
  filter with a count in the header.
- Search now digs through brand, size, color, notes, and category, and supports
  phrases like "owner 4/0". Added sorting and category browse cards with item counts.
- Removed the old salt/fresh lure filter; it described lure classes, not a tackle
  inventory, and categories + search cover it.
- Files: src/features/tackle/* (types, fixture, card, editor sheet, new choice-field
  and quantity-stepper, filter tests), docs/design/08-tackle-box.md,
  docs/specs/tackle-box.md, tackle page metadata.
- Follow-up in the same session: keyboard-only sweep (tabbing, Enter/Space, Escape,
  focus rings) passed 11/11 after one fix — confirming a delete used to strand keyboard
  focus on the page background; focus now moves into the confirm step and back, and the
  confirm text is announced to screen readers.
- Checked the spec's 5,000-item scale target with a throwaway stress fixture (removed
  after): the full list was slow to search on first keystrokes. The list now renders
  100 cards at a time with a "Show more" button and the favorites rail caps at 12
  cards; at 5,000 items the page loads in ~0.6s with ~1.5k DOM nodes and search stays
  snappy.
- Consistency pass after the owner's design review: hand-drawn stroke icons for all
  13 categories (always beside their text label), attributes moved out of the link
  color into a proper primary→muted hierarchy, the "session-only" notice quieted from
  a cyan box to a calm caption, one shared chip spec everywhere, fixed-width star
  Favorite pills, and Ready-to-rig rail cards made equal height on a raised surface so
  they read as a quick strip instead of a duplicate list.
- Owner round 3: removed the "Ready to rig" rail from the main page, and category
  cards now open a dedicated page per category (/tackle/hooks, /tackle/line, …) with
  a back link, its own counts, and Add gear that pre-picks the category. Inventory
  state moved to a small shared session store so edits survive moving between pages
  (still resets on refresh, as the notice says). Also fixed "1 items" grammar nits.
- This work is on its own branch/PR stacked on the category-driven inventory PR;
  merge that one first.
- Checked: verify suite green (tokens, tripwires, lint, types, 136 tests), production
  build passes, 46 automated browser checks pass at phone size (add/edit/duplicate/
  delete, search, sort, category navigation, session persistence across pages, 404
  for unknown category, 320px wrap) plus a 14-check keyboard-only pass. Not checked
  on a real phone yet.
- Still session-only on purpose: nothing saves after a refresh until the offline
  store lane lands. Photos and tags are still future work per the spec.
- Note for the next agent: this branch was cut from main before the tide-chart
  cleanup PR merges; the two branches do not share files.
