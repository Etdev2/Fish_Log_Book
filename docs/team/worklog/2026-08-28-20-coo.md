### 2026-08-28 | coo | 60m

- Started the production UI work. Founder chose "design system and app shell first,"
  and chose to split lanes with the ChatGPT/Codex session rather than duplicate work.
- Found about 2,650 lines of finished Learning Dashboard work that had never been saved
  to GitHub. It existed on one Mac and nowhere else. Saved it on its own branch before
  doing anything else. The Codex session turned out to be editing those same files while
  the snapshot was taken, which cost them nothing and put a floor under the work.
- Had the architect settle six structural questions that were blocking everyone. The big
  one: the repo had been using two different styling systems with nobody having chosen.
  Tailwind wins, the old files get converted once, and CI will stop new ones.
- Had the design specialist write the visual language a 70-year-old on a boat can use in
  glare with wet hands. Contrast ratios were computed rather than guessed.
- Caught two mistakes before they did damage. First was mine: I told the design
  specialist its folder was empty without checking, and it nearly deleted the index to
  eight existing screen mockups. Restored, and it then inherited those mockups' 68-88px
  button sizes instead of inventing its own. Second, both sessions were about to write
  today's log to the same filename, which would have destroyed the Codex session's log.
- Checked the Codex session's dashboard against the brief rather than trusting its log.
  All four guide steps, all four honest status labels, real focus handling, device-only
  feedback, no database writes, no secrets. Lint passes.
- Files: `docs/design/` (7 docs plus tokens.json),
  `docs/architecture/decisions/005-front-end-architecture.md`, worklogs 19 and 20.
- Still broken / still open:
  - No test framework exists in this project at all. Every enforcement rule the
    architect wrote is currently a wish, not a rule.
  - The eight mockups are drawn in Archivo. The app loads Geist. Nobody has picked.
    Waiting on the founder.
  - None of this has been opened in a browser or on a phone.
- Next: founder picks the font, then the design system gets built as real code.
