### 2026-08-28 | ux-ui | 1h 50m

- Wrote the production visual design language for Fish Log Book in `docs/design/`:
  color palette + exact contrast ratios (targeting AAA/7:1 for body text, justified
  against a 65-75-year-old reader in glare with no reading glasses), a type scale
  floored at 18px body / 16px absolute minimum, spacing, corner/elevation/border rules,
  semantic color roles (interactive, destructive, the amber "needs attention" flag,
  tide, moon, disabled, focus), touch/interaction law, specs for every V1 component
  (button, input, chip, card, toast, modal/sheet, empty/error/loading, the calendar day
  cell), the app shell, and an accessibility baseline with a "what does done mean" list.
- Caught and fixed my own mistake mid-task: `docs/design/` was not empty — it already
  held 8 approved screen mockups (`screens/*.dc.html`) plus a README I nearly
  overwrote. Restored the original README content (screen table, canvas link) and
  reframed the new docs as formalizing that precedent, not replacing it. In particular
  I'd invented a 56px primary-button size before checking the mockups; they'd already
  established 68-88px (up to 88px for `Log a Catch`, the highest-stakes tap in the
  app), so I adopted that as the working number everywhere instead of my invented one.
- After `architect`'s ADR 005 (front-end architecture) landed mid-task, went back
  through my component specs and removed/annotated every place that had assumed a
  component or icon library: modal/sheet now states native `<dialog>` is the
  implementation primitive, with Radix named as the one bounded exception only if
  native can't be made focus/screen-reader-correct (untested, flagged for head-dev);
  select similarly defaults to native `<select>`; the loading spinner is now
  explicitly "inline SVG plus a CSS `@keyframes` rule," not a library import. Linked
  to ADR 005 instead of restating its styling/token/directory decisions.
- Wrote `docs/design/tokens.json` — the proposed contents of
  `src/core/design/tokens.json` per ADR 005 §2, values only, for head-dev to move into
  place. Flagged plainly that this product is dark-only by design in V1 (a light
  theme would actively hurt readability in direct sun) and that every token repeats
  its dark value under `light` as an honest placeholder rather than leaving it unset.
- Left one decision open on purpose rather than resolving it silently: the mockups use
  Archivo + IBM Plex Mono, the shipped app currently loads Geist Sans/Mono. Stated
  both directions' real cost, recommended Archivo/Plex Mono, and routed the actual
  call to the founder in `docs/design/README.md` — this affects already-shipped
  `src/app/layout.tsx`, outside this role's write scope, and ADR 005 didn't touch it.
- Files: `docs/design/README.md`, `docs/design/01-foundations.md`,
  `docs/design/02-semantic-colors.md`, `docs/design/03-touch-and-interaction.md`,
  `docs/design/04-components.md`, `docs/design/05-app-shell.md`,
  `docs/design/06-accessibility-baseline.md`, `docs/design/tokens.json`.
- Next: the font-family question needs a founder answer before head-dev wires
  `tokens.json`'s `fontFamily` block for real. The Radix-vs-native test for
  dialog/select is unrun — someone needs to actually try native `<dialog>` in this
  codebase before assuming it clears the a11y bar. No screens were designed this
  round on purpose (scope was foundations + shell only) and none were built or run in
  a browser — this was a docs-only round with no code to check.
