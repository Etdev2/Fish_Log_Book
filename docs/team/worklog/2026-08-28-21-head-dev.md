### 2026-08-28 | head-dev | 55m

- Built the design-token pipeline from ADR 005 #2, and made the ADR's font decision
  (Archivo for UI, IBM Plex Mono for numbers/measurements) real code instead of a
  "pending" note.
- Moved `docs/design/tokens.json` to `src/core/design/tokens.json` — the real source of
  truth per the ADR. Deleted the `$note` key that described the move (its job is done).
  Kept `$darkOnlyByDesign` untouched — it's a real design rationale, not pipeline noise.
  Did not touch a single color, size, or spacing value; ux-ui still owns those.
- Filled in `fontFamily` (previously marked `$openQuestion`/pending) with the founder's
  settled choice: `var(--font-archivo), 'Helvetica Neue', sans-serif` for `ui`, and
  `var(--font-ibm-plex-mono), ui-monospace, monospace` for `mono`. Used CSS variables
  rather than the bare font names because `next/font/google` self-hosts fonts under a
  hashed internal family name — the token has to reference the variable layout.tsx
  defines, or the self-hosting optimization silently doesn't apply.
- Wrote `scripts/tokens.mjs` (plain Node ESM, no new deps). It reads the JSON and emits
  `src/app/tokens.generated.css`: a `@theme { ... }` block for Tailwind v4's utility
  vocabulary, plus a `prefers-color-scheme: dark` override block for the color tokens
  (currently identical values to light, per `$darkOnlyByDesign`, but the structure is
  real so a future light theme is a JSON edit, not a script change). Elevation tokens
  are composite (fill + border + shadow), so they come out as plain CSS custom
  properties rather than `@theme` entries — turning those into `@utility` classes is
  component work, out of scope here.
- One naming quirk worth flagging to ux-ui, not something I fixed: color tokens named
  `text-primary`, `text-muted`, `text-link` land in Tailwind's `--color-*` namespace as
  `--color-text-primary` etc., so the actual utility class for that text color is
  `text-text-primary`, not `text-primary`. That's mechanically correct and I didn't
  rename anything, but it reads oddly at the call site. Flagged, not decided.
- Wired `npm run tokens` into `package.json`, plus `predev`/`prebuild` so the generated
  CSS can never go stale relative to the JSON. `npm run build` runs it every time,
  matching the ADR's "verify in a production build" instruction.
- `src/app/globals.css` now does `@import "tailwindcss"; @import "./tokens.generated.css";`
  and nothing else — the old hardcoded `--background`/`--foreground`/Geist variables are
  gone. One source of design values in the repo, not two.
- Swapped fonts in `src/app/layout.tsx`: `Archivo` and `IBM_Plex_Mono` from
  `next/font/google`, same CSS-variable pattern the Geist setup already used. IBM Plex
  Mono isn't a variable font on Google Fonts, so I passed explicit weights
  (400/500/600/700); Archivo is variable and needs none.
- Touched `src/app/page.tsx` only to swap its existing Tailwind classes for token-backed
  ones (`bg-surface`, `text-text-primary`, `rounded-md`, `text-h1`, etc.) — no structural
  or content changes — as proof the tokens actually reach a component. No new routes or
  screens were built, per the constraint.
- Verified end to end: temporarily changed `--color-surface`'s value in
  `tokens.json`, reran `npm run tokens`, confirmed the generated CSS changed, then
  reverted and regenerated back to the real value. Also grepped the production build
  output (`.next/static/chunks/*.css`) and confirmed `bg-surface{background-color:var(--color-surface)}`
  and `--font-archivo:"Archivo","Archivo Fallback"` are actually present in the compiled
  CSS, not just the intermediate generated file.
- Checks run: `npm run lint` (clean), `npm run build` (clean, prebuild regenerates
  tokens first), `npx tsc --noEmit` (clean). All three pass.
- Files: `src/core/design/tokens.json` (new, moved), `docs/design/tokens.json` (deleted),
  `scripts/tokens.mjs` (new), `src/app/tokens.generated.css` (new, generated —
  do not hand-edit), `src/app/globals.css`, `src/app/layout.tsx`, `src/app/page.tsx`,
  `package.json`.
- Not done, deliberately, per scope: no `@utility` layer for elevation tokens, no CI
  drift-check step for `tokens.generated.css`, no `*.module.css` conversion (blocked on
  the other branch per the ADR), no tripwire for raw hex/arbitrary values in `.tsx`. All
  four are named as future work in ADR 005 and none were in this task's DONE list.
- I have looked at the generated CSS and the built output directly, not just run the
  build — I have not opened this in an actual browser window.
- Branch: `head-dev/design-tokens`, in worktree `/Users/et/dev/Fish_Log_Book-tokens`.
  Pushed.
