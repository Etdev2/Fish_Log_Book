### 2026-08-30 | ux-ui | 35m

- Added a realistic lunar surface texture inside a native SVG phase mask. It uses the
  astro engine's continuous illuminated fraction and mirrors waxing and waning, so the
  supplied visual direction remains legible from a compact calendar moon through full size.
- Linked the moon to the chart's centered time. A normal swipe now changes the moon and
  active date while leaving the inspected tide point alone.
- Added a horizontal calendar made only from the ten station days actually present in the
  cached prediction. Each date carries its own moon preview; tapping one moves the selected
  reading and curve together. Previous/next day buttons now use the same path.
- Moved the selected-point panel below the curve so the chart remains the visual focus, and
  tightened the new header so the calendar still uses 74px targets on the narrowest phone.
- Real Chrome checks at 320px and 375px found no page overflow, no control below 48px, and
  no browser errors. Automated touch swipes changed the chart moon from 96% to 80% without
  changing the selected reading; a calendar jump then changed both chart and moon to a 47%
  last-quarter day and kept the active calendar cell visible.
- Added four moon-geometry regression tests. A real iPhone/VoiceOver pass is still not run.
- Migrated the chart's legacy CSS Module into the approved global component layer. Full
  verification passed: architecture tripwires, lint, TypeScript, 109 tests, and the
  production webpack build. Final 320px and 375px Chrome passes loaded the texture in all
  22 moon instances with no overflow, undersized targets, or runtime errors.
- Files: `src/features/conditions/tide-chart.tsx`,
  `src/styles/tide-chart.css`,
  `public/images/moon-surface.png`,
  `src/features/conditions/components/moon-phase-visual.tsx`,
  `src/features/conditions/components/moon-phase-visual.test.ts`,
  `src/features/conditions/format.ts`, `docs/design/07-tide-chart.md`.
