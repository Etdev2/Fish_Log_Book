### 2026-09-01 | architect | Regulations docs-first: architecture, data model, verified SoCal pack v1

Track B of the joint Claude/GPT spec (founder ruling: docs-first; §18 gates UI).

- **Architecture document** (`docs/specs/regulations-architecture.md`): regulation = data
  + citation or nothing shown; the diary never implies law (ADR 007 reconciled, not
  contradicted); geography is CDFW's own areas verbatim with polygons bundled into the
  pack and resolved on device; the map decision landed — no interactive map in v1, MPA
  layer linked out to CDFW; staleness is an amber banner on a horizon (60d / 30d
  in-season), never hidden; "No verified data" is permanent copy, not a gap filler.
  PR #19's fate and salvage are written down in §9.
- **Data model** (`docs/specs/regulations-data-model.md`): `reg_area` (verbatim agency
  geography), `reg_group` (RCG, Paralabrax — the complexes the law itself speaks in),
  `reg_pack` (version), `reg_rule` (one enforceable statement per row: verbatim sentence
  + typed translation + provenance; sizes always carry their *measure* because CDFW
  writes both total and fork length and a number without its measure is not a rule).
  Exactly three queries defined; everything else is out of scope on purpose.
- **Starter dataset** (`20260901230000`): 38 verified rows covering the founder's home
  water — two CDFW areas, the halibut/Paralabrax/white-seabass/sheephead/whitefish/
  sculpin/leopard/yellowtail set, giant sea bass as a hard `prohibited` row, September
  2026 ocean salmon with its 20,000-fish harvest guideline flagged `check_inseason`
  (30-day staleness), and the messy truth of groundfish: RCG bag-of-10 with per-species
  exceptions, four zero-retention species, the descending-device rule, the shore/diver
  exemption, and the Southern GMA's *split* Oct–Dec windows (nearshore closed;
  shelf/slope offshore-of-the-50-fathom-line only). All verbatim quoted from
  wildlife.ca.gov with the pages' own update stamps (Southern region page: *today*,
  2026-09-01; groundfish summary: 2026-06-23).
- New species ids (ocean_whitefish, giant_sea_bass, kelp_greenling) added to SQL and the
  ontology mirror together; parity test enforces the join.
- No Postgres in this sandbox: migration arity machine-checked (38/38 rows match their
  column lists), FK targets verified against the seed vocabulary, parity + full suite
  (364) + lint + tsc green. CI holds the apply-the-chain test.
