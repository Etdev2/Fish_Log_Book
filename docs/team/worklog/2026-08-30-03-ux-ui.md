### 2026-08-30 | ux-ui | 15m

- Replaced the identical sunrise/sunset chart glyphs with conventional horizon markers:
  an amber upward transition for sunrise and an orange downward transition for sunset.
- Added the same directional cue to visible curve labels and split the chart key into
  “Sunrise · daylight begins” and “Sunset · darkness follows.”
- Made the moon header report the centered chart light state as Daylight, Twilight, or
  Night, with a second non-colour symbol, so swiping always has an explicit light answer.
- Replaced the moon visual's remaining raw colour literals with existing design tokens
  after the architecture gate surfaced them.
- Exact 320px and 375px Chrome passes showed both mirrored transition symbols clearly,
  no page overflow, no target below 48px, and no runtime errors. Architecture tripwires,
  lint, TypeScript, all 109 tests, and the production webpack build passed.
- Files: `src/features/conditions/tide-chart.tsx`, `src/styles/tide-chart.css`,
  `src/features/conditions/components/moon-phase-visual.tsx`,
  `docs/design/07-tide-chart.md`.
