### 2026-08-28 | ux-ui | 55m
- Added a mobile-first Learn & Build dashboard with a four-step customer guide and a Builder View that labels working, prototype, planned, and undecided work honestly.
- Added keyboard-safe dialogs, visible progress, replay/skip/exit controls, fictional catch/conditions/history samples, and device-only review notes with reviewer, status, comment, time, and reset controls.
- Corrected the first guide step so its status card is explicitly a tour representation with a link to the real app route, not a claim about the current live connection result.
- Made feedback loading validate every saved item and made saving transactional: the screen changes only after browser storage succeeds, and storage failures are shown visibly and announced.
- Restored focus to the guide launcher after close, skip, or finish; guide headings receive focus as steps change; mobile detail scrolling now respects reduced-motion preferences.
- Files: src/app/layout.tsx, src/app/page.tsx, src/app/learn/page.tsx, src/components/app-nav.tsx, src/components/app-nav.module.css, src/features/learning/learning-dashboard.tsx, src/features/learning/learning-dashboard.module.css
- Launched the first guide step over the real `/` entry and its server-rendered connection
  card; later unfinished screens remain clearly labeled mockups.
- Checks: `npm run lint`, `npx tsc --noEmit --incremental false`, and
  `npx next build --webpack` pass. `/learn` and `/?guide=1` return HTTP 200.
- Inspected the dashboard in headless Chrome at desktop and narrow/mobile widths. A real
  iPhone and Vercel deployment still need a smoke test after publishing.
