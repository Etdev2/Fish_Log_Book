### 2026-09-04 | head-dev | 40m

- The founder opened an older catch and there was no moon on it — no conditions section at
  all. Yesterday's lunar fix only helped catches logged from now on; every fish already in
  the log still had nothing stored, and nothing was ever going to fill it in.
- The moon does not need filling in. Its phase depends on nothing but the moment, so the
  reading worked out today is the same number that would have been written at the time.
  The record now computes it when the stored value is missing. Nothing new is saved — it
  is worked out for display, the same way the phase name already was.
- Also fixed a related emptiness: the conditions section used to disappear entirely when
  every field was blank, which is why that record had no section at all rather than an
  empty one. Since the moon is knowable for any catch that has a time — which is all of
  them — the section now always has something to say.
- Put the two lunar numbers behind one shared function so the log-time path and the
  view-time path cannot drift apart, and wrote a test that they agree for the same moment.
  That equivalence is the whole justification for computing it late, so it is worth
  pinning rather than trusting.
- Checked on a backfilled catch from 2 September with no GPS — the case from the
  screenshot. It now reads "Moon — Last quarter · 67% lit".
- Checks: tripwires clean, no lint errors, 616 tests, production build green.
- Files: src/core/rules/astro/moon.ts, index.ts, features/catches/conditions.ts,
  catch-record-view.tsx, catch-detail-client.tsx
