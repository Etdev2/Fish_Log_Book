### 2026-08-29 | head-dev | 45m

- Added a dev-only auth bypass so the founder can skip the magic-link sign-in loop
  while working locally. It only turns on when the app is not a production build AND
  the `DEV_AUTH_BYPASS=true` env var is set on the server — never in the browser, never
  in a deployed build. It logs a loud warning whenever it's active. Wrote
  `docs/development/dev-auth-bypass.md` explaining exactly how to turn it on and off.
- Removed the "Learn & Build" tab from the bottom navigation. Confirmed first that the
  onboarding tutorial inside it was already dead (its only button pushes to
  `/?guide=1`, and the calendar page never reads that param) — so nothing new became
  unreachable, the tab was the last live door into a room that was already empty.
  Left all the code on disk and compiling; left a comment on the page explaining how to
  bring the tab back later.
- Added "Tide" as the second tab in the bottom nav, so the tide chart is now reachable
  from the main navigation, not just a button on the calendar.
- Deleted `src/components/app-nav.tsx` and its CSS module — confirmed nothing imports
  them first. Had to also remove the now-stale entry for that CSS module from
  `scripts/check-tripwires.mjs`'s legacy list (that script explicitly asks the person
  who deletes the file to do this).
- Checked whether anything downstream assumes a signed-in Supabase user exists: no. The
  only other consumers of the Supabase client are the sign-in/callback pages and a
  settings diagnostics check that reports backend reachability, not user identity.
  Nothing would crash with an empty session.
- Ran `npm run verify` (tokens, tripwires, lint, tsc, vitest) clean.
- Started the dev server locally (had to use `--webpack` instead of Turbopack — this
  worktree's symlinked `node_modules` makes Turbopack panic with "Symlink points out of
  the filesystem root", unrelated to this change) and hit `/` with curl. Could not
  fully exercise the redirect behavior end-to-end: this environment's `.env.local` has
  no Supabase URL/key configured, so the proxy's auth gate is already a no-op
  (`if (!url || !key) return response`) regardless of the bypass. I read the bypass
  logic closely instead and I'm confident in it, but I did not personally see the
  redirect flip on/off in a browser with real credentials — flagging this so someone
  with working Supabase credentials in this worktree can do a final visual check.
- Files: src/proxy.ts, .env.local.example, docs/development/dev-auth-bypass.md,
  src/features/shell/components/shell-nav.tsx,
  src/features/shell/components/shell-nav.test.ts,
  src/app/(internal)/learn/page.tsx, src/app/(app)/page.tsx,
  scripts/check-tripwires.mjs, src/components/app-nav.tsx (deleted),
  src/components/app-nav.module.css (deleted).
- Next: someone with real Supabase credentials in this worktree should confirm the
  bypass toggles the redirect in a browser. `src/app/tides/` (the empty directory the
  brief asked me to delete) did not exist — only the live `src/app/(app)/tides/` does —
  so there was nothing to remove there.
