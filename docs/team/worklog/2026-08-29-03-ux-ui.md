### 2026-08-29 | ux-ui | 55m

Session cut short by the coordinator to force a push before I reached a working state.
This is a partial handoff, not finished work. **Do not merge this branch.**

What I actually did, against the eight numbered tasks in the brief:

1. **Engine refactor of tide-chart.tsx — NOT STARTED.** `tide-chart.tsx` is byte-for-byte
   unchanged. It still has its own private minutes-since-`TIDE_BASE_UTC` maths, its own
   `heightAt`/`tideLegAt`/rate/slack, and reads `TIDE_POINTS` directly. This is the biggest
   piece of the brief and the one everything else depends on, and it has not begun.
2. **Units (feet default) — partially done.** `src/features/settings/units.ts` is a real,
   working `useUnitPreference()` hook (localStorage-backed, feet default) and
   `UnitsToggle` is wired into `/settings` and renders. `src/features/conditions/format.ts`
   has the conversion functions (`formatHeight`, `formatRate`, etc.) ready to call. NONE
   of this is wired into the chart yet — the chart still prints raw metres via its own
   `meters()` helper. **Known debt:** the local store this preference should live in
   (`meta`, IndexedDB via `idb`, ADR 005 §6) does not exist in this worktree — `src/lib/`
   was out of bounds for this branch. `units.ts` is `localStorage` behind the same hook
   shape a real `meta`-backed version would have; only its internals need to change later.
3. **State/rate/pace surfacing — NOT STARTED** in the chart. `format.ts` has label maps
   for `TideMotion`/`PaceClass`/moon phase names ready to use.
4. **Slack markers/countdowns — NOT STARTED.** `format.ts` has `formatCountdown` /
   `formatDurationMagnitude` with the natural-language phrasing the brief asked for
   ("12 min to Slack", not "0h 12m"), unused so far.
5. **Current-time marker — NOT STARTED. No decision made yet** on what "now outside the
   fixture window" should look like. That is still open.
6. **Sun/moon seam — types and fixture written, NOT rendered.** `src/features/conditions/
   types.ts` mirrors the future `@/core/rules/astro` shapes (`DaylightSpan`, `SunEvents`,
   `MoonPhase`) without importing that module (it isn't merged). `astro-fixture.ts` has a
   hand-authored `daylightSpansFixture()` (contiguous/sorted/gap-free, Newport Beach,
   flat sunrise/sunset times, no DST edge in this window) and a real-but-approximate
   `moonPhaseFixture()` synodic-month calc. Neither is imported by any component yet — no
   shading, no moon badge exist.
7. **"Open tide chart" button on the home page — NOT decided/touched.** I looked at
   `src/features/shell/components/shell-nav.tsx` (out of bounds, read-only) and the
   architect's premise is currently false: `SHELL_ROUTES` only has Calendar, Learn &
   Build, Settings — **Tide is not a nav tab**. The home-page button is still the only
   way to reach `/tides`. My call, once I actually get to it: keep the button, and flag
   to architect/head-dev that Tide needs adding to `shell-nav.tsx` (their file, not
   mine) before that button can be safely removed.
8. **What not to build** — correctly not touched: no catch markers, no scoring.

New/changed files this commit (`src/app/(app)/page.tsx` NOT touched):
- `src/features/conditions/queries/tide-series.ts` — builds a `TidePredictionSeries` from
  `tide-fixture.ts` via the engine's `tideSeries()`. Untested against a running chart.
- `src/features/conditions/format.ts` — Intl + unit-conversion layer. Untested against a
  running chart; only verified via `tsc`.
- `src/features/conditions/types.ts`, `src/features/conditions/astro-fixture.ts` — the
  astro seam, unused so far.
- `src/features/settings/units.ts`, `src/features/settings/components/units-toggle.tsx` —
  working, wired into `/settings`, but not consumed by the chart.
- `src/app/(app)/settings/page.tsx` — added the "Height units" section.

**`npm run verify` result — FAILS, not fixed:**

```
> tokens:check   PASS
> tripwires      PASS (tripwires: clean.)
> lint           FAIL

/…/src/features/settings/units.ts
  51:5  error  Error: Calling setState synchronously within an effect can trigger
  cascading renders (react-hooks/set-state-in-effect)
```

The `useEffect` in `useUnitPreference()` calls `setUnit(readStored())` synchronously to
pick up the real preference after the SSR-safe default render — same pattern as
`backend-diagnostics.tsx`'s existing `useEffect` + `setState`, but the newer
`react-hooks/set-state-in-effect` rule (from `eslint-config-next`) flags it here and
apparently not there; I did not chase why. Not fixed — next session's first job on this
file. Likely fix: read the stored value directly as the initial `useState` value via a
lazy initializer (safe here because `localStorage` is genuinely unavailable during SSR
and the function already guards `typeof window === "undefined"`), and keep the effect
only for the event-subscription half. `tsc --noEmit` passes clean on its own (checked
separately, before wiring into the `verify` chain).

Never ran `npm run test`, `npm run build`, or opened the app in a browser this session —
did not get that far. `tide-chart.tsx` should still render exactly as before since it is
untouched, but I have not confirmed that by looking.

**Everything else in the brief (peak-shape write-up, Sourced lint-rule experience,
day/night contrast check, keyboard/320px/200%-zoom pass) — no findings, because none of
that code exists yet.**

- Files: src/features/conditions/queries/tide-series.ts, src/features/conditions/format.ts,
  src/features/conditions/types.ts, src/features/conditions/astro-fixture.ts,
  src/features/settings/units.ts, src/features/settings/components/units-toggle.tsx,
  src/app/(app)/settings/page.tsx
- Next: fix the `react-hooks/set-state-in-effect` lint error in `units.ts` first (small).
  Then start task 1 (engine refactor of `tide-chart.tsx`) — everything else in the brief
  depends on it and none of it can be verified in a browser until it lands. Do not merge
  this branch as-is; it is a checkpoint, not a deliverable.
