### 2026-08-28 | coo | 35m
- Reworked the eight existing AI roles into a thirteen-role operating system with clear
  LOW, MEDIUM, and HIGH defaults, narrow permissions, escalation paths, and context rules.
- Split testing, read-only review, Git integration, repository scouting, and diagram work
  out of the head developer so routine jobs use cheaper specialists and one role owns
  merges into `main`.
- Added matching project-scoped Claude Code and Codex agent configurations. Codex defaults
  unmatched child work to Luna/low and caps child concurrency at three.
- Audited the interrupted Claude worktrees. The previous house-rule change is already
  pushed and merged. Preserved an untracked biostat test-vector set and an unpushed
  architect WIP commit for separate validation; neither was silently merged.
- Files: `AGENTS.md`, `CLAUDE.md`, `.claude/agents/`, `.codex/`, and `docs/team/` policy files.
- Next: validate and preserve the two interrupted data/schema workstreams on their own
  branches before deciding whether either is ready to integrate.
