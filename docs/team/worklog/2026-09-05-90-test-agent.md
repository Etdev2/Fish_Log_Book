### 2026-09-05 | test-agent
- Added migration guardrail tests for existing Fish Games behavior before tournament-domain migrations land.
- Protected event deduplication, sequence-based ordering, append-only void history, frozen Fish Legal snapshots, guest identity compatibility, and deterministic scoring.
- Files: `src/core/rules/games/migration-guardrails.test.ts`
- No production behavior was changed.
- Tests were authored from the existing scoring/types contracts; this GitHub-only lane did not execute the local `npm run verify` command, so CI/local verification is still required before merge.
