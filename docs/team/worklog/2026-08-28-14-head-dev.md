
### 2026-08-28 | head-dev | 45m
- Fixed the thing that caused every merge conflict this project has had. The team log and
  the message board were each one big file that every assistant added to at the bottom, so
  whenever two of them worked at the same time they both changed the same last line and
  the project refused to combine their work. Split both into one small file per entry.
  Separate files cannot collide, so that whole category of problem is gone.
- Nothing was lost in the split — I checked the old and new text match exactly before
  deleting anything. Thirteen work entries and thirty-one messages, all still there.
- Changed the rule that said nobody may combine their own work into the main copy.
  Assistants may now do it themselves once the automatic checks pass. The old rule made a
  second assistant re-read the entire project just to nod along, which costs the founder
  real money and catches less than the automatic check does.
- Added the rule that matters most for someone who runs out of usage mid-session: send
  your work to the online repository before you stop, finished or not. Writing it down on
  this computer is not the same as it being saved, and a session can end mid-sentence.
- Also added: create your branch when you start a job, not when you finish. Today an
  assistant described a fix as in progress on a branch that did not exist.
- Wrote a plain-English git cheat sheet for the founder, including the one command that
  answers "is my work actually saved?"
- Files: docs/team/worklog/ (new, 14 files), docs/team/channel/ (new, 31 files),
  docs/team/WORKLOG.md and docs/team/CHANNEL.md (now index pages, not logs),
  docs/team/HOUSE-RULES.md, docs/team/GIT-CHEATSHEET.md (new)
- Honest notes: no application code touched, so nothing to look at in a browser. The two
  database bugs found yesterday in the mark-resolution trigger and the journal entry
  constraint are still unfixed and still have no branch.
- Next: fix those two database bugs, and get the SQL run against the real Supabase project
  rather than the throwaway one it was tested in.
