### 2026-08-30 | ux-ui | 50m

- Audited the two completed Claude tide-chart passes before changing anything. Kept the
  timezone, null-state, daylight, provenance, and accessibility fixes they had already
  landed.
- Rebuilt the tide page around a phone-first hierarchy: station and units first, one
  focused chart card, then a separate “Right now” conditions card. Exploring another
  day no longer changes the current conditions summary.
- Restored the visible feet/metres choice, removed the accidental second layer of phone
  padding, aligned hour labels to real three-hour clock boundaries, strengthened the
  curve, and replaced the cramped status-cell grid with readable full-width events.
- Fixed the chart’s touch model. A quick swipe pans with momentum without changing the
  selected point; a 260ms hold enters inspection mode. Mouse hover/click and keyboard
  arrow/Home/End paths remain available.
- Checked the real page in Chrome at exact 320px and 375px widths. At 320px there was no
  horizontal overflow, no measured interactive target below 48px, and no browser error.
  Automated touch input confirmed swipe-to-pan and hold-to-inspect behave differently.
- `npm run verify` passed (105 tests). `npm run build -- --webpack` passed. The default
  Turbopack build wrote a completed build trace but did not exit while the existing
  long-running dev server had the same `.next` directory open, so the documented webpack
  build path was used for the clean production-build check.
- Files: `src/features/conditions/tide-chart.tsx`,
  `src/features/conditions/tide-chart.module.css`.
- Still not checked with VoiceOver on a physical iPhone; DOM order, names, focus rings,
  keyboard behavior, reduced-motion styling, and touch sizing were checked in Chrome.
