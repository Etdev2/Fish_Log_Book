### 2026-08-29 | ux-ui | 45m

Two founder items, addressed in order. `npm run verify` green. Server still running at
**http://localhost:3413/tides**. Left the day-band shading contrast question alone —
told explicitly it's with the founder as an open decision, not mine to act on.

**1. Readout pill moved out of the plot.** It was an absolutely-positioned HTML overlay
sitting above the SVG; now it's plain inline text in the card header row, immediately
right of the date `<h2>`, reading the same live `reading` value the status grid uses
(still updates live while dragging). At <640px it wraps onto its own full-width line
below the date (`basis-full`) rather than squeezing the day-nav buttons — the top row
at that width is back to exactly two elements (date, nav), same as before this whole
feature existed. At >=640px it sits inline between the date and the nav group, sharing
space with the date (both `min-w-0 shrink truncate`) while the nav group stays
`shrink-0` and pinned right. Dropped the bordered pill styling for plain header
typography, since nothing floats there anymore. Removed the now-dead `dragging` state
that only existed to style the old overlay. `PLOT_TOP` dropped 22 -> 20, the actual
floor set by the in-SVG orange SELECTED pill's own geometry — the removed HTML overlay
was `position: absolute` and never reserved layout height, so that alone freed nothing;
this second pixel is a separate, small, real gain.

**2. "Now" button.** Replaced the middle nav button (previously "return to the fixture's
hardcoded anchor instant") — that anchor's only reason to exist was compensating for
"now" being outside the data, which stopped being true once the fixture was refreshed to
span today. Reuses the same `selectAt`/`scrollToAt` path everything else already uses,
including its `prefers-reduced-motion` handling — no second scrolling mechanism.
Disabled in both honest failure cases: clock not yet resolved (`useNow()` returns `null`
during SSR/first hydration — confirmed in the rendered HTML: `disabled`, `title="Waiting
for the clock."`), and current instant outside the loaded window (this fixture will age
past Sep 4 eventually) — a `title` plus `aria-describedby` pointing at a `sr-only` span
explain why in both cases; it never scrolls to a clamped edge and implies that edge is
"now". `aria-pressed` reflects when the read-head is already within a minute of now.

- Files: src/features/conditions/tide-chart.tsx, tide-chart-geometry.ts
- Next: still owed — an actual browser/screenshot pass (320px, 200% zoom, keyboard,
  VoiceOver). Everything reported so far has been verified by reading the rendered HTML
  and reasoning about the CSS, not by looking at pixels.
