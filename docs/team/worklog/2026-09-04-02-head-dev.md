### 2026-09-04 | head-dev | 1h30m

- Found the real reason catch records opened to a blank page. It was one wrong word.
- The screen that shows sunrise and sunset asked the browser to format those times in a
  time zone called "local". There is no such time zone — the browser throws an error at
  that point — and because it happens while the page is drawing, the whole record
  disappears rather than just that one line.
- It only ever happened on catches that had a GPS fix, because sunrise and sunset are only
  worked out when the app knows where you were. That is exactly why I could not reproduce
  it yesterday: every test catch I made had no fix, so the broken line was never reached.
  The founder's screenshot showed GPS on all four fish, which was the clue.
- Fixed twice over, on purpose. The screen now asks for the time zone the catch was
  actually logged in — which is also more correct, since reading sunrise in some other
  zone would be wrong even if it had worked. And the time formatter itself now falls back
  to the phone's own zone instead of throwing, so a bad zone can never take down a record
  again. Four tests hold both halves down.
- Confirmed with a catch that has a GPS fix: the record opens and the sun line reads
  "rise 6:27 AM · set 7:14 PM".
- Two honest notes. My local production test server got into a bad state partway through
  and started serving a stale build, so the final check was done against the development
  server rather than a production one. And I saw a warning in the logs about the species
  list rendering "white seabass" twice; I could not reproduce it — the underlying data has
  no duplicates — so it is written down here rather than fixed on a guess.
- Checks: tripwires clean, no lint errors, 607 tests, production build green.
- Files: src/features/catches/format.ts, catch-record-view.tsx, catch-detail-client.tsx
- Next: the duplicate-species warning above wants a proper look.
