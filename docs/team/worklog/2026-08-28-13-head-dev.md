
### 2026-08-28 | head-dev | 30m
- Fixed the two schema bugs I found while running the migrations in pglite, as a new
  migration (`20260828130000_v1_catch_resolution_and_journal_fixes.sql`) rather than
  editing the four already-merged files — nothing has run against the real project yet,
  but rewriting applied migration history is a habit that bites later regardless.
- Fix 1: `tg_catch_resolution` now fires `BEFORE INSERT OR UPDATE`, not just `UPDATE`.
  A catch inserted straight to `confirmed` (D22's "full catch form" entry point, not
  just the quick-mark path) now gets `resolved_at`/`resolved_by` auto-filled the same
  way a later UPDATE does, without the client having to set them by hand. The
  cannot-return-to-unresolved guard still only checks OLD on UPDATE, since OLD does not
  exist on INSERT.
- Fix 2: `journal_entry`'s uniqueness on `(angler_id, entry_date)` is now a partial
  unique index (`where deleted_at is null`) instead of a table constraint. A
  soft-deleted day's entry no longer permanently blocks writing a new one for that
  date — D23 makes the day page the primary writing surface, so this would have
  surfaced fast and looked exactly like data loss.
- Re-ran all five migration files together in pglite (same throwaway WASM Postgres
  approach as before, not the real project) with a purpose-built regression check for
  each bug, plus three extra probes on D21-D24 pieces that had never been executed:
  the sticky-rig snapshot-on-insert plus its append-only guard (D21a), and the
  `capture_mode` live-window CHECK bounds on both `trip` and `catch`, including the
  exact ±boundary seconds and the immutability trigger. All passed. Full detail in the
  channel note to `architect`/`coo`.
- Files: supabase/migrations/20260828130000_v1_catch_resolution_and_journal_fixes.sql
  (new)
- Honest notes: this is still pglite, not the real Supabase project — that machine/
  password gap from the last entry is unchanged. Branch is `head-dev/schema-fixes`,
  not merged — per house rules I don't merge my own work; asked for a second pair of
  eyes since I wrote the fix.
