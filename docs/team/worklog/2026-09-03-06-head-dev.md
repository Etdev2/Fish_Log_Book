# 2026-09-03 — Head Dev — session on the flush path

Writes stamp `cachedAnglerId()` (auth.uid once the cookie exists). Sender refreshes the
session, upserts `public.angler`, overwrites `angler_id` on the wire, and strips unknown
columns. SQL: signup trigger + catch extras (`species_other`, `quantity`, `tags`,
`favorite`). Field test still needs a real project — see `docs/development/supabase-project.md`.
